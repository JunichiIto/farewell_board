%h1 My messages
%p
  -# = link_to 'New Message', new_user_message_path(current_user.id), class: "btn"
- if @messages.any?
  %table.table.table-bordered.table-condensed
    %thead
      %tr
        %th(width="70px")
        %th(width="120px") To
        %th Text
    %tbody
      - @messages.each do |message|
        %tr
          %td{:style=>'text-align: center'}
            = link_to 'Edit', edit_user_message_path(current_user, message), class: "btn"
            -# = link_to 'Destroy', user_message_path(current_user, message), :confirm => 'Are you sure?', :method => :delete
          %td= "#{message.to_user.name}さん"
          %td= message.text
- else
  %p
    No messages.
